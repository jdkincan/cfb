# Runtime dependencies for cfbmeta. Deliberately small: this installs on every
# scheduled run, so anything not actually imported stays out.
requests==2.32.5
Jinja2==3.1.6
PyYAML==6.0.2
numpy==2.3.2

# Tests only, but tiny and needed by the CI checks workflow.
pytest==8.3.4
